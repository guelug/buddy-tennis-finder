{
  "name": "matchpoint-iap-verifier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "types:check": "wrangler types worker-configuration.d.ts --env-interface WorkerBindings --check",
    "typecheck": "npm run types:check && tsc --noEmit"
  },
  "dependencies": {
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "^4.121.0"
  }
}
